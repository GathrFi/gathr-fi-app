import 'package:flutter/material.dart';

import '../../../shared/assets/colors.gen.dart';

class ExpenseUtilityTooltip extends StatelessWidget {
  const ExpenseUtilityTooltip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(color: ColorName.border);
  }
}
